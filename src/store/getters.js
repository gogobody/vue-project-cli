const getters = {
  // doneTodos: state => {
  //   return state.todos.filter(todo => todo.done)
  // }
  // chargeDetail(state) {
  //   return state.parkType.chargeDetail
  // }
}

export default getters
